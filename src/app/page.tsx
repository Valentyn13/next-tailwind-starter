import { cn } from '@/utils';

const Home = () => {
    return (
        <div className={cn('h-full', 'w-full', 'flex')}>
            <h1>Hellow world</h1>
        </div>
    );
};

export default Home;
